import 'package:flutter/material.dart';

class ChooceInterestScreen extends StatelessWidget {
  const ChooceInterestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('data'),),
    );
  }
}